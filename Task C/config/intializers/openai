# OpenAI configuration
# Make sure to set OPENAI_API_KEY environment variable
# You can do this by:
# 1. Creating a .env file in the root directory
# 2. Or setting it as an environment variable in your system

# For development, you can use dotenv gem if installed:
require 'dotenv/load' if defined?(Dotenv)

